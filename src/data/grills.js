export default class {
/**
  * constructor method
  * @param brand
  * @param series
  * @param name
  * @param fuel
  * @param size
  * @param rented
*/
  constructor( brand, series, name, fuel, size, rented ){
    this.brand = brand;
    this.series = series;
    this.name = name;
    this.fuel = fuel;
    this.size = size;
    this.rented = rented;
  }
}
