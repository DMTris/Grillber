export default class {
/**
  * constructor method
  * @param brand
  * @param series
  * @param name
  * @param fuel
  * @param size
*/
  constructor( brand, series, name, fuel, size ){
    this.brand = brand;
    this.series = series;
    this.name = name;
    this.fuel = fuel;
    this.size = size;
  }
}
