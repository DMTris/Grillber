export default class {
/**
  * constructor method
  * @param brand
  * @param series
  * @param name
  * @param fuel
  * @param size
  * @param price
  * @param rented
*/
  constructor( brand, series, name, fuel, size, price, rented ){
    this.brand = brand;
    this.series = series;
    this.name = name;
    this.fuel = fuel;
    this.size = size;
    this.price = price;
    this.rented = rented;
  }
}
